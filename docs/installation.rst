============
Installation
============

At the command line::

    $ easy_install openstack-env

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openstack-env
    $ pip install openstack-env